generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Expense {
  id       Int      @id @default(autoincrement())
  amount   Float
  category String
  date     DateTime @default(now())
}